# YouTube Recorder · P1 Spike 结论

> By Leoluchino · 2026-07-19
> 结论：**MacWhisper Watch Folder + SRT 主路径可行，批准进入 P2。**

## 验证环境

- MacWhisper 14.2 Pro，Watch Folder 指向 `~/Coding/YouTube Recorder/macwhisper-inbox`，导出格式 Subtitles (.srt)，Auto-Transcribe 开启
- yt-dlp 2026.07.04（用户 Mac，pip --user 安装）；沙盒网络无法直连 YouTube，下载必须在本机执行（与生产环境一致）
- 真实频道：@MeiTouNews（UC 解析成功）、@RhinoFinance（解析成功）

## 关键发现

1. **无字幕假设成立**：MeiTouNews 最新视频 `nnze4i2Mt6o`（18 分钟）人工字幕和自动字幕**都没有**。审计判断正确——转录是主路径，不是兜底。
2. **Watch Folder 工作正常**：音频落地后数秒内被检测；22 秒测试音频 ~30 秒出稿；18 分钟真实视频 **~2.5 分钟**出稿（约 7 倍实时速度）。
3. **SRT 落在 inbox 同一文件夹**（`{video_id}.srt` 与 `{video_id}.m4a` 并排）。设计中的独立 outbox 取消，适配器直接监视 inbox 中的同名 `.srt`。
4. **时间码质量好**：501 段、单调递增、语音覆盖 67%（其余为静音/音乐，正常）；中文财经术语识别准确（"52周高点""20日均线高出8.3%"）。
5. **发现两个必须处理的缺陷**（验证了审计的 Transcript Validator 必要性）：
   - 尾部幻觉段：最后一段 "Продолжение следует..."（Whisper 经典结尾幻觉）；
   - 该段时间戳 1105.7s **超出视频总长** 1078s。
   → Validator 规则：丢弃超出 `duration + 容差` 的段；检测已知幻觉模式（纯外语孤立短句、重复循环段）。
6. **音频下载**：yt-dlp `-f 140`（原生 m4a，无需 ffmpeg）18 分钟视频 25 秒内完成，17.4 MB。
7. 专有名词误识（"Leoluchino"→"Lea Lachino"）：交给 MacWhisper 术语表或文章阶段 LLM 校正。

## 未验证项（顺延）

- SRT 时间码 vs 真实画面偏移：需要 ffmpeg 抽帧，归入 P6 视觉管线首个任务（届时 `brew install ffmpeg`）。
- `mw` CLI 时间码能力：主路径已通，CLI 降为可选调研。
- RhinoFinance 会员视频（第293期）：确认存在会员内容，管线需按"永久错误"分类跳过。

## 对 v0.2 设计的修订

- 删除 `macwhisper-outbox`；监视逻辑 = inbox 中出现与音频同名的 `.srt`。
- Transcript Validator 增加：时长越界裁剪 + 尾部幻觉检测（本次实测样本已能触发）。
- 依赖清单：ffmpeg 从"必装"改为"P6 起必装"（音频下载用 `-f 140` 不需要它；API 备用路径的压缩/切块仍需要）。

## 样本档案

`spike-results/`：watchtest.m4a/.srt（冒烟测试）、nnze4i2Mt6o.m4a/.srt（真实无字幕视频）。
